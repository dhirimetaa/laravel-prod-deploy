{
    "scripts": {
        "deploy:build": "prod-deploy build",
        "deploy:push": "prod-deploy push",
        "deploy:push:target": "prod-deploy push:target",
        "deploy:vendor-push": "prod-deploy vendor:push",
        "deploy:vendor-zip": "prod-deploy vendor:zip",
        "deploy:vendor-zip-push": "prod-deploy vendor:zip-push",
        "deploy:migrate": "prod-deploy migrate",
        "deploy:optimize": "prod-deploy optimize",
        "deploy:artisan": "prod-deploy artisan",
        "deploy": [
            "@deploy:build",
            "@deploy:push"
        ]
    }
}
